{
  "name": "todolist",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install"
  },
  "engines": {
    "node": "18.x"
  },
  "author": "",
  "license": "ISC",
  "description": "Todo List Application with Angular + Node.js + PostgreSQL",
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
