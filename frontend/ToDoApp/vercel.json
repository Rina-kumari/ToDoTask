{
  "buildCommand": "npm install && npx ng build --output-path=dist/browser",
  "outputDirectory": "dist/browser",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}